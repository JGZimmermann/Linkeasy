<div class="row mb-3">
  <div class="col-8">
    <div class="input-group">
      <input type="text" class="form-control" placeholder="Procurar tag" aria-label="Procurar tag">
      <button class="btn btn-outline-primary" type="button">Pesquisar...</button>
    </div>
  </div>
  <div class="col-4 text-end">
    <a href="<?= route('tags.new') ?>" class="btn btn-outline-success">
      <i class="fe fe-plus"></i> Nova Tag
    </a>
  </div>
</div>

<div class="d-flex flex-wrap gap-2">
  <?php foreach ($tags as $tag): ?>
    <div class="d-inline-flex align-items-center bg-primary text-white px-3 py-2 rounded-pill shadow-sm">
      <span class="me-2"><?= htmlspecialchars($tag->name) ?></span>
      <form action="<?= route('tags.destroy', ['id' => $tag->id]) ?>" method="POST" class="d-inline m-0">
        <input type="hidden" name="_method" value="DELETE">
        <button type="submit" class="btn btn-sm btn-link text-white p-0 m-0">
          <i class="bi bi-x-circle"></i>
        </button>
      </form>
    </div>
  <?php endforeach; ?>
</div>
